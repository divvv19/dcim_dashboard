# Testing

- No automated testing framework (Jest, Cypress, etc.) is currently configured in `package.json`.
- Testing appears to be manual, with mock data simulation built directly into the backend (`server.js`) for UI development.
