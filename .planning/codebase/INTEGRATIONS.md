# Integrations

## Hardware / Industrial Protocols
- **Modbus**: Integration intended via `modbus-serial` library (listed in backend package.json).
- **SNMP**: Integration intended via `net-snmp` library.

## Network / Real-time
- **Socket.IO**: Used for bidirectional real-time data streaming between the backend interface and the React frontend.
