---
phase: 01-device-onboarding-protocol-connectivity
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/onboarding/contracts.js
  - server/src/state/deviceStateStore.js
  - server/src/profiles/profileCatalog.js
  - server/src/onboarding/validators.js
  - server/test/onboarding/contracts.test.js
  - server/test/onboarding/validators.test.js
autonomous: true
requirements:
  - INVT-01

must_haves:
  truths:
    - "Onboarding enforces required metadata: site, rack, device type, protocol, and profile assignment."
    - "Duplicate protocol endpoints are detected via canonical endpoint keys before any save attempt."
    - "Unknown device models are represented as Pending with polling disabled."
    - "Device lifecycle status vocabulary is explicit and constrained to Pending, Tested, Active, Failed."
    - "Profile templates for AC, UPS, and sensors are versioned and allow only constrained overrides."
  artifacts:
    - path: "server/src/onboarding/contracts.js"
      provides: "Canonical onboarding data model and lifecycle status definitions."
    - path: "server/src/state/deviceStateStore.js"
      provides: "Indexed in-memory device state with endpoint-key lookup support."
    - path: "server/src/profiles/profileCatalog.js"
      provides: "Curated Modbus/SNMP profile templates and version metadata."
    - path: "server/src/onboarding/validators.js"
      provides: "Required field, duplicate endpoint, and override allowlist validation rules."
  key_links:
    - from: "server/src/onboarding/validators.js"
      to: "server/src/profiles/profileCatalog.js"
      via: "Validation resolves profile template identity/version before accepting input."
      pattern: "profileCatalog\\.(get|resolve|list)"
    - from: "server/src/state/deviceStateStore.js"
      to: "server/src/onboarding/contracts.js"
      via: "Store enforces lifecycle/status and endpoint key invariants from onboarding contracts."
      pattern: "OnboardingStatus|normalizeEndpointKey"
---

<objective>
Establish backend onboarding foundations: data contracts, lifecycle-safe state store, and strict profile/metadata validation.

Purpose: Reduce implementation risk for INVT-01 by locking safety-critical invariants before API/runtime wiring.
Output: Foundational onboarding modules with tests proving duplicate blocking, lifecycle constraints, and profile policy.
</objective>

<execution_context>
@C:/Users/user/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/user/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-device-onboarding-protocol-connectivity/01-CONTEXT.md
@.planning/phases/01-device-onboarding-protocol-connectivity/01-RESEARCH.md

<interfaces>
From `server/server.js` (existing realtime contract):
```javascript
io.emit('dashboard:update', stateStore);
socket.emit('dashboard:update', stateStore);
```

From `src/hooks/useRealtimeData.js` (frontend subscription contract to preserve):
```javascript
socket.on('dashboard:update', (newData) => {
  setData(newData);
  setLastUpdated(new Date());
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define onboarding contracts, state store, and lifecycle invariants</name>
  <files>server/src/onboarding/contracts.js, server/src/state/deviceStateStore.js, server/test/onboarding/contracts.test.js</files>
  <behavior>
    - Creating a device draft with missing required metadata is rejected.
    - Status transition rules allow Pending -> Tested -> Active and set Failed on connection-test failure.
    - Unknown model registration remains Pending and flagged `pollingEligible=false`.
  </behavior>
  <action>Create explicit onboarding contracts for device metadata, endpoint identity, profile reference, and lifecycle status. Implement an in-memory device store with indexed lookup by device id and canonical endpoint key. Enforce lifecycle transition guards and `pollingEligible` flag semantics to support later scheduler integration.</action>
  <verify>
    <automated>node --test server/test/onboarding/contracts.test.js</automated>
  </verify>
  <done>Contracts and state store are implemented with passing tests for required metadata, lifecycle transitions, and unknown-model Pending behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement profile catalog and strict onboarding validation rules</name>
  <files>server/src/profiles/profileCatalog.js, server/src/onboarding/validators.js, server/test/onboarding/validators.test.js</files>
  <behavior>
    - Modbus and SNMP profiles are selected from a curated template catalog by device class.
    - Limited overrides are accepted only for an allowlisted subset while preserving template identity/version.
    - Duplicate endpoint key detection blocks creates and returns merge guidance.
  </behavior>
  <action>Build a versioned template catalog for AC, UPS, and sensor classes (Modbus + SNMP). Implement validator helpers for required onboarding fields (site, rack, device type, protocol, profile assignment), canonical endpoint identity generation (`modbus:host:port:unitId`, `snmp:host:port:version:credentialKey`), and constrained profile overrides. Include explicit unsupported-metric metadata when required register/OID mappings are absent.</action>
  <verify>
    <automated>node --test server/test/onboarding/validators.test.js</automated>
  </verify>
  <done>Catalog and validators enforce required metadata, duplicate endpoint blocking with merge hints, and template version/override policy with tests passing.</done>
</task>

</tasks>

<verification>
Run `node --test server/test/onboarding/contracts.test.js server/test/onboarding/validators.test.js` and confirm onboarding foundation tests pass.
</verification>

<success_criteria>
Backend onboarding foundations exist for AC/UPS/sensor registration rules: required metadata, profile assignment constraints, duplicate endpoint blocking, and explicit Pending/Tested/Active/Failed lifecycle semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/01-device-onboarding-protocol-connectivity/01-01-SUMMARY.md`
</output>
