---
phase: 01-device-onboarding-protocol-connectivity
plan: "05"
type: execute
wave: 3
depends_on:
  - 01-02
  - 01-04
files_modified:
  - server/src/polling/pollingPolicy.js
  - server/src/polling/connectorState.js
  - server/src/polling/pollScheduler.js
  - server/src/realtime/snapshotProjector.js
  - server/server.js
  - server/test/polling/pollScheduler.test.js
autonomous: true
requirements:
  - INTG-01
  - INTG-02

must_haves:
  truths:
    - "Active Modbus and SNMP devices are polled using tiered interval/timeout defaults with per-device overrides."
    - "Polling failures follow bounded exponential backoff and mark devices offline after configured consecutive failures."
    - "Device freshness is computed and projected as fresh/stale in emitted realtime snapshots."
    - "Unsupported metrics from adapters are preserved in emitted payloads and never silently synthesized."
  artifacts:
    - path: "server/src/polling/pollScheduler.js"
      provides: "Per-device polling loop with bounded backoff, no overlap, and lifecycle-aware scheduling."
    - path: "server/src/realtime/snapshotProjector.js"
      provides: "Projection of onboarding + runtime connector state for dashboard payload consumers."
    - path: "server/server.js"
      provides: "Runtime wiring for onboarding routes, poll scheduler, and dashboard:update emissions."
  key_links:
    - from: "server/src/polling/pollScheduler.js"
      to: "server/src/protocols/protocolAdapter.js"
      via: "Scheduler uses protocol adapter contract to execute test/collect operations by protocol."
      pattern: "collectSample|testConnection"
    - from: "server/src/realtime/snapshotProjector.js"
      to: "server/server.js"
      via: "Projected connector state is emitted through dashboard:update."
      pattern: "io\\.emit\\('dashboard:update'"
---

<objective>
Wire protocol adapters into polling runtime, connector health state, and realtime snapshot emission.

Purpose: Complete INTG-01 and INTG-02 operational behavior by connecting adapter primitives to resilient runtime execution.
Output: Poll scheduler, connector state/freshness logic, snapshot projection, and server bootstrap integration.
</objective>

<execution_context>
@C:/Users/user/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/user/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-device-onboarding-protocol-connectivity/01-CONTEXT.md
@.planning/phases/01-device-onboarding-protocol-connectivity/01-RESEARCH.md
@.planning/phases/01-device-onboarding-protocol-connectivity/01-02-PLAN.md
@.planning/phases/01-device-onboarding-protocol-connectivity/01-04-PLAN.md

<interfaces>
From `server/src/onboarding/onboardingService.js` (from 01-04):
```javascript
listPollingEligibleDevices();
updateDeviceRuntimeState(deviceId, patch);
```

From `server/src/protocols/protocolAdapter.js` (from 01-02):
```javascript
getProtocolAdapter(protocol);
adapter.testConnection(device, profile);
adapter.collectSample(device, profile);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement poll policy, runtime connector state, and scheduler backoff behavior</name>
  <files>server/src/polling/pollingPolicy.js, server/src/polling/connectorState.js, server/src/polling/pollScheduler.js, server/test/polling/pollScheduler.test.js</files>
  <behavior>
    - Device-class defaults apply: UPS 5s/1500ms, AC 10s/1500ms, sensor 15s/1200ms.
    - Retry delay follows `baseInterval * min(2^failCount, 8)` with bounded jitter.
    - Offline state triggers after 3 consecutive failed cycles; freshness turns stale after `2 * baseInterval`.
  </behavior>
  <action>Implement tiered polling policy defaults with per-device override support. Build one-shot per-device scheduling (no overlapping polling loops), bounded exponential retry/backoff on failures, success reset behavior, and connector runtime state (`online/offline`, `failCount`, `lastSuccessAt`, `freshness`). Skip polling for devices not eligible from onboarding lifecycle state.</action>
  <verify>
    <automated>node --test server/test/polling/pollScheduler.test.js</automated>
  </verify>
  <done>Scheduler enforces interval/timeout policy, retry/backoff, offline threshold, and freshness state with passing automated tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Project runtime state to dashboard snapshots and integrate server bootstrap wiring</name>
  <files>server/src/realtime/snapshotProjector.js, server/server.js</files>
  <behavior>
    - `dashboard:update` payload includes lifecycle, connectivity, freshness, and unsupported metric details per device.
    - Polling runtime starts and stops according to onboarding lifecycle transitions.
    - Onboarding API routes and runtime emission path coexist without breaking existing socket channel semantics.
  </behavior>
  <action>Integrate onboarding routes (from 01-04), adapter runtime (from 01-02), and scheduler state into `server/server.js`. Add snapshot projector logic that merges onboarding records, connector state, and latest samples into an inventory-oriented payload for frontend consumption. Emit updates on lifecycle transitions, poll failures/offline transitions, and successful sample collection.</action>
  <verify>
    <automated>node --test server/test/polling/pollScheduler.test.js && node server/server.js</automated>
  </verify>
  <done>Server runtime emits coherent realtime snapshots with trust indicators and starts cleanly with onboarding + connector runtime wired together.</done>
</task>

</tasks>

<verification>
Run `node --test server/test/polling/pollScheduler.test.js` and confirm scheduler behavior passes.
Smoke-run `node server/server.js`, verify runtime initialization logs, then terminate process.
</verification>

<success_criteria>
Platform executes resilient Modbus/SNMP polling and emits device-level lifecycle/connectivity/freshness/unsupported-metric state over `dashboard:update` for downstream UI consumption.
</success_criteria>

<output>
After completion, create `.planning/phases/01-device-onboarding-protocol-connectivity/01-05-SUMMARY.md`
</output>
