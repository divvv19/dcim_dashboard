---
phase: 01-device-onboarding-protocol-connectivity
plan: "04"
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - server/src/onboarding/onboardingService.js
  - server/src/onboarding/onboardingRoutes.js
  - server/test/onboarding/onboardingService.test.js
  - server/test/onboarding/onboardingRoutes.test.js
autonomous: true
requirements:
  - INVT-01

must_haves:
  truths:
    - "Registration enforces connection test before a device can be saved as Active."
    - "Duplicate endpoint attempts return merge guidance instead of creating a second record."
    - "Unknown models can be saved as Pending and remain excluded from polling eligibility."
    - "Editing an existing device re-runs full validation and requires re-test before Active restore."
  artifacts:
    - path: "server/src/onboarding/onboardingService.js"
      provides: "Onboarding orchestration for test-before-save, duplicate handling, and lifecycle transitions."
    - path: "server/src/onboarding/onboardingRoutes.js"
      provides: "REST route handlers for test connection, create, update, and list device records."
    - path: "server/test/onboarding/onboardingRoutes.test.js"
      provides: "Endpoint-level verification of expected onboarding request/response behavior."
  key_links:
    - from: "server/src/onboarding/onboardingRoutes.js"
      to: "server/src/onboarding/onboardingService.js"
      via: "Route handlers delegate onboarding orchestration to service methods."
      pattern: "onboardingService\\.(testConnection|createDevice|updateDevice|listDevices)"
    - from: "server/src/onboarding/onboardingService.js"
      to: "server/src/state/deviceStateStore.js"
      via: "Service persists validated onboarding records and lifecycle state updates."
      pattern: "deviceStateStore\\.(create|update|find|list)"
---

<objective>
Implement the onboarding API orchestration layer and route surface on top of Phase 1 foundation rules.

Purpose: Complete actionable INVT-01 backend behavior while keeping runtime scheduler wiring isolated for a follow-up plan.
Output: Onboarding service + routes with tests validating test-before-save, duplicate blocking, Pending handling, and edit revalidation.
</objective>

<execution_context>
@C:/Users/user/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/user/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-device-onboarding-protocol-connectivity/01-CONTEXT.md
@.planning/phases/01-device-onboarding-protocol-connectivity/01-RESEARCH.md
@.planning/phases/01-device-onboarding-protocol-connectivity/01-01-PLAN.md

<interfaces>
From `server/src/onboarding/validators.js` (from 01-01):
```javascript
validateOnboardingInput(payload);
buildEndpointKey(payload);
```

From `server/src/state/deviceStateStore.js` (from 01-01):
```javascript
createDevice(record);
updateDevice(deviceId, patch);
findByEndpointKey(endpointKey);
listDevices();
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build onboarding service orchestration for create/update/test workflows</name>
  <files>server/src/onboarding/onboardingService.js, server/test/onboarding/onboardingService.test.js</files>
  <behavior>
    - Registration executes connection test before persisting Active devices.
    - Failed tests produce Failed status and block final activation.
    - Device edits trigger complete validation and re-test before returning to Active.
  </behavior>
  <action>Implement onboarding service methods for `testConnection`, `createDevice`, `updateDevice`, and `listDevices` using validator/state-store foundations from 01-01. Enforce duplicate endpoint blocking with merge hint payloads, Pending behavior for unknown models, and lifecycle outcomes (`Pending`, `Tested`, `Active`, `Failed`). Keep protocol test execution behind an injectable adapter hook so runtime integration can be wired in 01-05.</action>
  <verify>
    <automated>node --test server/test/onboarding/onboardingService.test.js</automated>
  </verify>
  <done>Service orchestration enforces onboarding lifecycle, duplicate handling, and test-before-save semantics with passing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose onboarding REST routes with endpoint-level behavior tests</name>
  <files>server/src/onboarding/onboardingRoutes.js, server/test/onboarding/onboardingRoutes.test.js</files>
  <behavior>
    - `POST /api/devices/test-connection` returns test result and lifecycle implication.
    - `POST /api/devices` and `PATCH /api/devices/:id` enforce service validation outcomes.
    - `GET /api/devices` exposes inventory records for frontend consumption.
  </behavior>
  <action>Create Express router handlers that delegate to onboarding service methods and return stable JSON error/success contracts for duplicate merge hints, Failed test outcomes, and Pending registration paths. Cover route behavior with tests using a minimal Express app harness.</action>
  <verify>
    <automated>node --test server/test/onboarding/onboardingRoutes.test.js</automated>
  </verify>
  <done>Onboarding routes are implemented with passing route tests and stable contracts expected by frontend onboarding/inventory flows.</done>
</task>

</tasks>

<verification>
Run `node --test server/test/onboarding/onboardingService.test.js server/test/onboarding/onboardingRoutes.test.js` and confirm onboarding API behavior passes.
</verification>

<success_criteria>
Backend exposes fully usable onboarding APIs with strict test-before-save and duplicate/merge behavior, while remaining decoupled from final scheduler runtime wiring.
</success_criteria>

<output>
After completion, create `.planning/phases/01-device-onboarding-protocol-connectivity/01-04-SUMMARY.md`
</output>
