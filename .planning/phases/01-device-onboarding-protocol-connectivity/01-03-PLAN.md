---
phase: 01-device-onboarding-protocol-connectivity
plan: "03"
type: execute
wave: 4
depends_on:
  - 01-04
  - 01-05
files_modified:
  - src/services/deviceApi.js
  - src/components/onboarding/DeviceOnboardingWizard.jsx
  - src/components/onboarding/DeviceInventoryPanel.jsx
  - src/components/onboarding/OnboardingStatusBadge.jsx
  - src/components/onboarding/FreshnessBadge.jsx
  - src/components/onboarding/UnsupportedMetricsList.jsx
  - src/hooks/useRealtimeData.js
  - src/DCIM.jsx
  - src/App.css
autonomous: true
requirements:
  - INVT-01
  - INTG-01
  - INTG-02

must_haves:
  truths:
    - "Operator can complete guided onboarding for one device at a time and must provide site/rack/device-type/protocol/profile."
    - "Operator must run and pass connection test before final save for Active devices."
    - "Inventory/onboarding screens display lifecycle status (Pending/Tested/Active/Failed) and per-device freshness (fresh/stale)."
    - "Duplicate endpoint responses are shown with merge guidance rather than silent failure."
    - "Unsupported metrics are visible in device detail instead of hidden."
  artifacts:
    - path: "src/components/onboarding/DeviceOnboardingWizard.jsx"
      provides: "Guided single-device onboarding UX with mandatory test-before-save sequence."
    - path: "src/components/onboarding/DeviceInventoryPanel.jsx"
      provides: "Inventory table/cards with connectivity and freshness indicators."
    - path: "src/services/deviceApi.js"
      provides: "API client for onboarding and inventory endpoints."
    - path: "src/hooks/useRealtimeData.js"
      provides: "Realtime ingestion of backend snapshot payload including inventory/connectors."
  key_links:
    - from: "src/components/onboarding/DeviceOnboardingWizard.jsx"
      to: "src/services/deviceApi.js"
      via: "Wizard test/save actions call onboarding endpoints."
      pattern: "deviceApi\\.(testConnection|createDevice|updateDevice)"
    - from: "src/hooks/useRealtimeData.js"
      to: "src/DCIM.jsx"
      via: "Hook returns inventory/connectivity state rendered in onboarding/inventory views."
      pattern: "useRealtimeData\\("
    - from: "src/components/onboarding/DeviceInventoryPanel.jsx"
      to: "src/components/onboarding/FreshnessBadge.jsx"
      via: "Freshness state rendered per device."
      pattern: "<FreshnessBadge"
---

<objective>
Deliver operator-facing onboarding and inventory UX for Phase 1, wired to the new backend onboarding/protocol runtime.

Purpose: Complete Phase 1 user outcomes by making registration, status lifecycle, connectivity freshness, and unsupported mappings visible in the dashboard.
Output: React onboarding/inventory components, API client integration, and realtime UI wiring.
</objective>

<execution_context>
@C:/Users/user/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/user/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-device-onboarding-protocol-connectivity/01-CONTEXT.md
@.planning/phases/01-device-onboarding-protocol-connectivity/01-RESEARCH.md
@.planning/phases/01-device-onboarding-protocol-connectivity/01-04-PLAN.md
@.planning/phases/01-device-onboarding-protocol-connectivity/01-05-PLAN.md

<interfaces>
From `src/hooks/useRealtimeData.js` (existing return contract to extend, not break):
```javascript
return { data, isConnected, lastUpdated };
```

From backend payload contract (from plan 01-05):
```javascript
dashboardSnapshot.inventory.devices[]
dashboardSnapshot.inventory.connectorStateByDeviceId
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build guided single-device onboarding wizard with mandatory test-before-save</name>
  <files>src/services/deviceApi.js, src/components/onboarding/DeviceOnboardingWizard.jsx, src/components/onboarding/OnboardingStatusBadge.jsx</files>
  <behavior>
    - Wizard cannot submit until site, rack, device type, protocol, and profile are all provided.
    - Connection test is required before enabling final save for Active flow.
    - Duplicate endpoint response displays merge hint and prevents creation.
  </behavior>
  <action>Create a dedicated onboarding API client (`testConnection`, `createDevice`, `updateDevice`, `listDevices`) and a guided wizard focused on one device per flow. Include protocol profile selection during registration, constrained override inputs, and explicit Pending path for unknown models. Keep lifecycle states visible in form feedback (`Pending`, `Tested`, `Active`, `Failed`).</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Operators can complete guided onboarding with required metadata, run test-before-save, and see duplicate/merge and lifecycle feedback in the wizard.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement inventory/connectivity panel with freshness and unsupported metric visibility</name>
  <files>src/components/onboarding/DeviceInventoryPanel.jsx, src/components/onboarding/FreshnessBadge.jsx, src/components/onboarding/UnsupportedMetricsList.jsx</files>
  <behavior>
    - Each onboarded device shows lifecycle and online/offline connectivity state.
    - Freshness badge switches between fresh/stale from backend freshness values.
    - Unsupported mappings are rendered explicitly per device/profile.
  </behavior>
  <action>Build an inventory panel (table or cards) that renders onboarding metadata (site, rack, device type, protocol, profile version), lifecycle state, connectivity state, freshness badge, last-seen timestamp, and unsupported metric list. Include action affordance for editing devices; editing flow must signal that re-validation + re-test is required.</action>
  <verify>
    <automated>npm run build && npm run lint</automated>
  </verify>
  <done>Inventory view shows per-device operational trust indicators (status/connectivity/freshness/unsupported metrics) with no hidden mapping gaps.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire onboarding and inventory views into DCIM realtime dashboard</name>
  <files>src/hooks/useRealtimeData.js, src/DCIM.jsx, src/App.css</files>
  <behavior>
    - Realtime hook ingests backend inventory snapshot updates without breaking existing metric views.
    - DCIM navigation includes onboarding/inventory sections visible alongside current dashboard tabs.
    - UI gracefully handles offline socket state with visible warning and stale data cues.
  </behavior>
  <action>Extend `useRealtimeData` to expose normalized inventory/connectivity payload alongside existing data shape, then integrate onboarding/inventory components into `DCIM.jsx`. Preserve current dashboard visuals while adding operator workflow entry points and status/freshness badges in onboarding/inventory areas. Add minimal styling updates in `App.css` to keep new sections responsive on desktop/mobile.</action>
  <verify>
    <automated>npm run build && npm run lint</automated>
  </verify>
  <done>Dashboard includes a functional onboarding/inventory workflow backed by realtime backend state while existing tabs remain operational.</done>
</task>

</tasks>

<verification>
Run `npm run build` and `npm run lint` to confirm frontend integration quality.
Run frontend + backend together and verify onboarding/inventory sections receive live `dashboard:update` status changes.
</verification>

<success_criteria>
Operators can onboard devices with required metadata and profile assignment, validate connectivity before activation, and monitor lifecycle/connectivity/freshness states from the DCIM UI.
</success_criteria>

<output>
After completion, create `.planning/phases/01-device-onboarding-protocol-connectivity/01-03-SUMMARY.md`
</output>
