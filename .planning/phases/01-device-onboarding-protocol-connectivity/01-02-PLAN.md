---
phase: 01-device-onboarding-protocol-connectivity
plan: "02"
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - server/src/protocols/protocolAdapter.js
  - server/src/protocols/modbusAdapter.js
  - server/src/protocols/snmpAdapter.js
  - server/test/protocols/modbusAdapter.test.js
  - server/test/protocols/snmpAdapter.test.js
autonomous: true
requirements:
  - INTG-01
  - INTG-02

must_haves:
  truths:
    - "Modbus devices can be connection-tested and sampled via a shared adapter contract."
    - "SNMP devices can be connection-tested and sampled via the same adapter contract."
    - "Adapter outputs include explicit `unsupportedMetrics` markers when mappings are missing or invalid."
    - "Timeout and profile-driven mapping options are accepted per device/profile configuration."
  artifacts:
    - path: "server/src/protocols/protocolAdapter.js"
      provides: "Common protocol adapter interface for test and collect operations."
    - path: "server/src/protocols/modbusAdapter.js"
      provides: "Modbus implementation using profile-driven register mappings."
    - path: "server/src/protocols/snmpAdapter.js"
      provides: "SNMP implementation using profile-driven OID mappings and varbind checks."
  key_links:
    - from: "server/src/protocols/modbusAdapter.js"
      to: "server/src/protocols/protocolAdapter.js"
      via: "Modbus adapter conforms to shared adapter contract."
      pattern: "testConnection|collectSample"
    - from: "server/src/protocols/snmpAdapter.js"
      to: "server/src/protocols/protocolAdapter.js"
      via: "SNMP adapter conforms to shared adapter contract."
      pattern: "testConnection|collectSample"
---

<objective>
Implement protocol adapter implementations for Modbus and SNMP with strict mapping/error semantics.

Purpose: Deliver INTG-01 and INTG-02 protocol primitives before poll scheduling and server runtime wiring.
Output: Adapter contract, concrete Modbus/SNMP modules, and protocol-focused unit tests.
</objective>

<execution_context>
@C:/Users/user/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/user/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-device-onboarding-protocol-connectivity/01-CONTEXT.md
@.planning/phases/01-device-onboarding-protocol-connectivity/01-RESEARCH.md
@.planning/phases/01-device-onboarding-protocol-connectivity/01-01-PLAN.md

<interfaces>
From `server/src/onboarding/contracts.js` (from 01-01):
```javascript
OnboardingStatus;
normalizeEndpointKey(deviceInput);
```

From `server/src/profiles/profileCatalog.js` (from 01-01):
```javascript
resolveProfileTemplate({ deviceType, protocol, profileId, version });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define protocol adapter boundary and implement Modbus adapter</name>
  <files>server/src/protocols/protocolAdapter.js, server/src/protocols/modbusAdapter.js, server/test/protocols/modbusAdapter.test.js</files>
  <behavior>
    - Modbus adapter `testConnection` and `collectSample` honor configured timeout and mapping definitions.
    - Adapter reports unsupported metrics when required register mappings are absent.
    - Connection test requires at least one required mapped read success.
  </behavior>
  <action>Create a shared adapter contract (`testConnection`, `collectSample`) and implement the Modbus adapter using installed `modbus-serial`. Accept interval/timeout/profile configuration from device/profile input and return structured sample payloads with `unsupportedMetrics[]` when mappings are missing or invalid.</action>
  <verify>
    <automated>node --test server/test/protocols/modbusAdapter.test.js</automated>
  </verify>
  <done>Modbus adapter conforms to shared contract, enforces mapping semantics, and passes protocol unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement SNMP adapter with varbind-safe collection semantics</name>
  <files>server/src/protocols/snmpAdapter.js, server/test/protocols/snmpAdapter.test.js</files>
  <behavior>
    - SNMP adapter validates varbind-level errors and closes sessions on success and failure paths.
    - Adapter honors configured profile options (version, timeout, retries, backoff).
    - Missing required mapping marks metric as `unsupported` in adapter output.
  </behavior>
  <action>Implement SNMP adapter using installed `net-snmp` with explicit session option handling and varbind error checks. Ensure session lifecycle cleanup happens in both pass/fail paths. Return normalized sample payloads aligned with the shared adapter contract and explicit `unsupportedMetrics[]` reporting.</action>
  <verify>
    <automated>node --test server/test/protocols/snmpAdapter.test.js</automated>
  </verify>
  <done>SNMP adapter provides connection-test and sample collection behavior with strict error handling and passing tests.</done>
</task>

</tasks>

<verification>
Run `node --test server/test/protocols/modbusAdapter.test.js server/test/protocols/snmpAdapter.test.js` and confirm both protocol adapter suites pass.
</verification>

<success_criteria>
Protocol adapters for Modbus and SNMP exist with consistent contract behavior, explicit unsupported metric signaling, and configurable profile/timeout handling required for runtime scheduling.
</success_criteria>

<output>
After completion, create `.planning/phases/01-device-onboarding-protocol-connectivity/01-02-SUMMARY.md`
</output>
