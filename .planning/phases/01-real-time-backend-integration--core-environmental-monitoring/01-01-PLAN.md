---
wave: 1
depends_on: []
files_modified: ["server/server.js", "package.json"]
autonomous: true
---

# Phase 1: Real-time Backend Integration & Core Environmental Monitoring
## Plan 1.1: Modbus & SNMP Implementation

<objective>
Replace the simulation loop in `server/server.js` with real `modbus-serial` and `net-snmp` integrations to fetch data from PDUs, UPS, and CRAC units.
</objective>

<requirements>
- REQ-1.1: Fetch facility data using Modbus/SNMP (`modbus-serial`, `net-snmp`).
</requirements>

<tasks>
  <task id="1">
    <description>Add necessary backend dependencies (`modbus-serial`, `net-snmp`) to the backend `package.json`.</description>
    <validation>The dependencies should be present in `server/package.json`.</validation>
  </task>
  <task id="2">
    <description>Initialize a generic Modbus RTU/TCP client class wrapper in `server.js` based on `modbus-serial` capable of reading holding registers for a given IP/port.</description>
    <validation>A wrapper class handling Modbus connectivity and basic reading should exist.</validation>
  </task>
  <task id="3">
    <description>Initialize a generic SNMP v1/v2c client wrapper in `server.js` based on `net-snmp` capable of fetching a given OID.</description>
    <validation>A wrapper class handling SNMP sessions should exist.</validation>
  </task>
  <task id="4">
    <description>Replace the `simulateData` interval polling block in `server.js` with calls to the new Modbus and SNMP wrappers, substituting the `mockState` objects with real data. Include placeholder IP addresses and registers as constants for now.</description>
    <validation>The simulation `setInterval` block should be removed and replaced with async polling functions.</validation>
  </task>
</tasks>

<context>
- These changes should run independently from the frontend to allow testing purely at the backend level before displaying the data.
</context>

<must_haves>
- The backend simulation loop is permanently removed.
- Valid wrapper classes for `modbus-serial` and `net-snmp` are established.
- The state objects (`upsData`, `coolingData`) are populated by resolving asynchronous Modbus/SNMP tasks.
</must_haves>
