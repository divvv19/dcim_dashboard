---
wave: 3
depends_on: ["01-01-PLAN.md", "01-02-PLAN.md"]
files_modified: ["server/server.js", "package.json"]
autonomous: true
---

# Phase 1: Real-time Backend Integration & Core Environmental Monitoring
## Plan 1.4: Persistent History

<objective>
Integrate a time-series database (e.g., InfluxDB) to store historical sensor data, replacing the transient array memory logic in `server.js`.
</objective>

<requirements>
- REQ-1.4: Standardize persistent historical sensor storage via a Time-Series Database (e.g., InfluxDB).
</requirements>

<tasks>
  <task id="1">
    <description>Add the `@influxdata/influxdb-client` dependency to the backend `package.json`.</description>
    <validation>Package successfully added to dependencies.</validation>
  </task>
  <task id="2">
    <description>Configure `InfluxDB` client initialization in `server.js` utilizing environment variables for URL, Token, Organization, and Bucket (fallback to localhost setups for development).</description>
    <validation>The backend successfully instantiates an InfluxDB WriteApi instance.</validation>
  </task>
  <task id="3">
    <description>Inject a point-writing function at the bottom of the Modbus/SNMP polling loop (from Wave 1/2) that maps the current `envData`, `upsData`, and `coolingData` objects to InfluxDB Point schemas and flushes them to the DB continuously.</description>
    <validation>The integration script properly maps variables to fields/tags and commits writes.</validation>
  </task>
  <task id="4">
    <description>Modify the backend Socket.IO listener for `request_history` (or create it) to perform an InfluxQL/Flux query retrieving the last 1-hour windows of metrics instead of referencing a local array, then broadcasting to the fontend.</description>
    <validation>The frontend successfully fetches and hydrates history charts from the TSDB layer instead of a mock array.</validation>
  </task>
</tasks>

<context>
- This relies on Wave 1 and Wave 2 being completed so that the data shapes (including new sensors) are finalized before creating DB schemas.
</context>

<must_haves>
- The backend `history` array is completely purged.
- The InfluxDB client writes data on each polling cycle.
- The React history graphs are still populated properly by pulling DB queried data.
</must_haves>
