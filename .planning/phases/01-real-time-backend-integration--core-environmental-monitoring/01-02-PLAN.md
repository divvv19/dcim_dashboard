---
wave: 2
depends_on: ["01-01-PLAN.md"]
files_modified: ["server/server.js", "src/DCIM.jsx"]
autonomous: true
---

# Phase 1: Real-time Backend Integration & Core Environmental Monitoring
## Plan 1.2: Enhanced Sensor Support

<objective>
Expand the `envData` model to include Airflow, Air Pressure, Smoke, and Water Leak contact closure sensors on both the backend generator and frontend UI views.
</objective>

<requirements>
- REQ-1.2: Implement an extended `envData` model matching new parameters (Airflow, Air Pressure, Smoke, Water Leak).
</requirements>

<tasks>
  <task id="1">
    <description>Extend the initial state object `envData` in `server/server.js` to include standard fields for: `airflow` (CFM), `pressure` (Pa), `smokeDetected` (boolean), `waterLeak` (boolean).</description>
    <validation>The state object clearly defines these properties.</validation>
  </task>
  <task id="2">
    <description>Update the backend Modbus/SNMP polling loop (created in Plan 1.1) to include simulated or mock read logic specifically targeting these new sensor fields to ensure real-time stream completeness.</description>
    <validation>The Socket.IO emitter includes these new fields in the data payload over the network.</validation>
  </task>
  <task id="3">
    <description>Modify the `EnvironmentView` and `Card` components inside `src/DCIM.jsx` to render the newly added sensor variables (`airflow`, `pressure`, `smokeDetected`, `waterLeak`) within the Environment metrics grid.</description>
    <validation>The frontend UI successfully maps and displays the new attributes alongside Temperature and Humidity.</validation>
  </task>
</tasks>

<context>
- Must run in Wave 2 as it relies on the backend polling structure established in Plan 1.1.
</context>

<must_haves>
- The backend `envData` model broadcasts six sensor metrics instead of two.
- The `EnvironmentView` in the frontend accurately visualizes the boolean and numerical additions without layout breakage.
</must_haves>
